import { ethers } from "ethers";
import { Config } from "./types";
export declare const fetchAbiAt: (address: string, { provider, rpcUrl, network, apiKey }: Config) => Promise<{
    name: string;
    abi: any;
}>;
export declare const getContractAt: (address: string, signerOrProvider?: ethers.providers.JsonRpcProvider | ethers.Signer) => Promise<ethers.Contract>;
